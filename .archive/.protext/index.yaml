# Protext Extraction Index
# Max 20 extractions. Triggers are keyword hints (suggest-mode default).

extractions:
  shell-layers:
    source: CLAUDE.md
    triggers: [shell, layer, profile, bashrc, zshrc, zsh, bash]
    summary: "Shell config hierarchy: .profile → shell RCs"
    tokens: ~300

  templates:
    source: CLAUDE.md
    triggers: [template, tmpl, chezmoi, go template]
    summary: "Chezmoi template patterns and naming conventions"
    tokens: ~200

  nodes:
    source: .chezmoi.toml.tmpl
    triggers: [node, cluster, crtr, drtr, trtr, cooperator, director, terminator]
    summary: "Cluster node definitions and architecture flags"
    tokens: ~400

  plugins:
    source: .chezmoiexternal.toml
    triggers: [plugin, zsh-plugin, external, fzf-tab, autosuggestions]
    summary: "External Zsh plugin versions and sources"
    tokens: ~300
